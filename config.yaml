terminal:
  backend: local
  cwd: /data/workspace
  timeout: 180
compression:
  enabled: true
  threshold: 0.85
model:
  default: mimi
  provider: openai-api
agent: {}
onboarding:
  seen:
    profile_build_offered: true
